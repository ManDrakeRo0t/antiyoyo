spring:

  application:
    name: antiyoyo

  rabbitmq:
    host: ${RABBITMQ_HOST:localhost}
    username: ${RABBITMQ_USERNAME:myuser}
    password: ${RABBITMQ_PASSWORD:secret}

  data:
    mongodb:
      uuid-representation: standard
      host: ${MONGO_HOST:192.168.0.5}
      authentication-database: admin
      database: ww-game
      username: ${MONGO_USERNAME:gen_user}
      password: ${MONGO_PASSWORD:o7;zGs516}u(jl}

#logging:
#  level:
#    root: DEBUG
#    org.springframework.web: DEBUG
